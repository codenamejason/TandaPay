const expect = require("expect");
const request = require("supertest");
const { app } = require("../index");

suite("POST /signup", () => {
  test("base test", () => {
    expect(true);
  });
});

teardown("Tearing down tests", () => {});
