const styles = theme => ({
    container: {
        display: "flex",
        justifyContent: "center",
    },
    card: {
        width: "70%",
        height: theme.spacing(30),
    },
});

export default styles;
