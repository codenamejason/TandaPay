const styles = (theme) => ({
	title: {
		color: "#22685D",
		textAlign: "center",
		fontWeight: "bold",
		fontSize: theme.spacing(5)
	},
	subtitle: {
		textAlign: "center",
		color: "darkgrey"
	},
	access: {
		display: "flex"
	}
});

export default styles;
