const styles = theme => ({
    container: {
        display: "flex",
        justifyContent: "center",
    },
    card: {
        width: "80%",
        padding: 25,
    },
});

export default styles;
